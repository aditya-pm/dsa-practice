#include <bits/stdc++.h>
using namespace std;

// Type aliases (shorter names)
// using ll = long long;
// using pii = pair<int, int>;
// using vi = vector<int>;

// Solve ONE test case
void solve() {
    // write solution here
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t = 1;
    cin >> t;  // comment this line if problem has single test case
    while (t--) {
        solve();
    }
    return 0;
}
